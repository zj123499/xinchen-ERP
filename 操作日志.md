# 新辰未来 ERP 操作日志

---

## 2026-08-01 代码清理 + 架构收尾

### 代码清理
| 文件 | 清理内容 |
|------|---------|
| `menus.ts` | 移除死代码 `flattenMenus()`、`filterMenusByCodes()`（无引用） |
| `employees/page.tsx` | 移除未使用的 `useRouter` 导入 |
| `partners/page.tsx` | 移除未使用的 `useRouter` 导入 |
| `commissions/page.tsx` | 移除未使用的 `useRouter` 导入 |

---

## 2026-08-01 部门架构混合方案

### 部门拆分
- 市场部拆为 **新媒体部** / **市场部** / **网络部** 三个独立部门
- 每个部门的角色登录后只看到本部门菜单
- 侧边栏 Logo 下方显示部门名称（蓝色标签）

### 角色→部门映射
| 部门 | 角色 | 菜单项数 |
|------|------|:--:|
| 新媒体部 | 新媒体主管/运营 | 3 |
| 市场部 | 市场专员 | 3 |
| 网络部 | 网络运营/直播 | 4 |
| 咨询部 | 升学顾问 | 8 |
| 交付部 | 文书申请 | 4 |
| 财务部 | 财务 | 7 |
| 管理 | admin/总经理/运营总监 | 全量 |

### 业绩记录
- 新增 `WorkRecord` 表（`work_records`），字段：userId/dept/type/amount/quantity/relatedId
- 新增 `GET/POST /api/work-records`
- 管理员看全部，员工只看自己的记录
- 这是后续**自动薪资计算**的基础数据

---

## 2026-08-01 全平台权限管控上线

### API 权限
- 80 个 API 文件添加 `requirePermission()`
- 新增 98 项权限码（seed 24→122）
- 菜单侧新增 `template`/`server` 图标

### 权限码覆盖
站群/营销/产品/回访/风险/AI/扩展/合作方/线索/跟进/系统设置 全覆盖。

---

## 2026-08-01 Nextcloud 文件存储集成

### 方案
- Tailscale 点对点 VPN：ERP 服务器 ↔ Nextcloud 本地机
- 存储后端切换为 Nextcloud WebDAV

### 文件夹规则
```
erp/contract_file/{合同ID}_{合作方名称}/
erp/partner_contract/{合作方ID}_{名称}/
erp/student_material/{学生ID}_{姓名}/
  ├── 成绩单/
  ├── 文书/
  ├── offer/          ← Offer 归入学生文件夹
  └── 其他/
```
- Offer 文件改为存入学生文件夹（`student_material` 统一管理）
- 合同文件删除补 `storage.remove()` 防 Nextcloud 孤文件

---

## 2026-08-01 公司品牌更名

### Logo 更换
- 全局替换为「Newill 新辰未来」Logo
- 位置：登录页 / 侧边栏 / 浏览器标签 / BI大屏 / 页面 Title
- 新增 `public/logo.svg`、`src/app/favicon.ico`

---

## 2026-08-01 AI 模块删除

### 删除
- 菜单中移除 AI 智能组（选校/文书/客服）
- `seed.ts` 移除 `ai:use` 权限码
- API 移除 `requirePermission("ai:use")`
- 数据库清除残留 `role_menus` 中 AI 关联

---

## 2026-07-30~31 关键修复

### 登录 502 修复
- `middleware.ts` 重命名为 `proxy.ts`（适配 Next.js 16）
- 重定向 URL 改用 Host 头，避免 `0.0.0.0` 问题

### 工作台优化
- 静默刷新：60 秒自动拉取数据，不显示 loading
- 移除切回标签页刷新
- 删除快捷操作模块
- 修复 React #418 SSR 水合错误（`toLocaleString` → 正则格式化）

---

## 2026-07-29 全面代码优化与部署

### 代码优化
| # | 文件 | 修改内容 |
|---|------|---------|
| 1 | `applications/page.tsx` | 移除未使用的 `router` 变量和 `useRouter` 导入 |
| 2 | `middleware.ts` | cookie `httpOnly` 统一为 `false`（与 login API 保持一致） |
| 3 | `api/auth/set-cookie/route.ts` | 增加 token 有效性验证，杜绝任意设置 cookie 漏洞 |

### 本月累积更新（2026-07-23 ~ 07-29）

#### 登录系统
- 登录页品牌名改为「新辰未来」
- 修复登录后跳回空白页问题：改用 form POST + 服务端 302 重定向设 cookie
- 中间件支持 `_t` URL 参数作为 cookie 兜底认证

#### 申请与 Offer 管理
- Offer 管理合并到申请管理页面，同一表格行展示 + 展开 Offer 列表
- 新增 Offer 表单内联弹窗，支持上传文件
- Offer 列表状态中文化（已收到/已接受/已拒绝）
- 新增截止日期列（最早 Offer 截止，≤7 天标红）
- 申请意向选中高亮修复（从硬编码 idx===0 改为 selectedIntentionId 追踪）
- **取消申请中的合同关联**：前端表单/表格/API 均移除 contractId 必填

#### 数据字典
- 全局硬编码下拉框解耦到数据字典（跟进方式/咨询业务/学生状态/性别/回访方式/增购类型/租房支付方式/合同状态/合作方类型）
- 分组支持中文显示名称，空分组保留
- 拖拽排序替代编码排序
- 刷新后保持滚动位置和展开状态

#### 审批功能
- 完整删除（菜单/页面/API/Schema/引用全部清理，净减 1146 行）

#### 菜单结构
```
改后：
合作方管理           ← 一级菜单
├── 合作院校
└── 合作方

扩展业务
├── 租房管理
└── 境外服务

交付管理
├── 申请管理          ← Offer 合并到此处
├── 签证管理
└── 回访管理
```

#### 签证
- 删除 `递交日期`/`签证号`/`有效期至`，新增 `批复日期`

#### 新媒体账号
- 新增字段：运营专员（下拉所有在职员工）、手机号、实名认证（输入框）、关联公司、人设风格（数据字典）、风格详情

#### 站群管理
- 新增字段：备案公司、法人代表

#### 租房管理
- 新增支付方式（年付/半年付/三个月）、合作方选择

#### 学生档案
- 新增删除按钮 + 级联清理关联数据

#### 其他修复
- 所有弹窗去除点击空白关闭，仅保存/取消按钮可关闭
- 跟进记录弹窗加关闭按钮
- 工作台自动刷新（每 30 秒 + 切回标签页）
- 服务器 Docker 缓存清理（回收 95GB）
- 删除文书工作台菜单

### GitHub
- 推送失败：SSH/HTTPS 均被防火墙拦截，需手动 `git push origin main`

### 部署状态
- **服务器**: 111.229.72.128:3000
- **容器**: xinchen-erp ✅ 运行中
- **登录**: admin / admin123
