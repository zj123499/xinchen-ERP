# 新辰未来 ERP 操作日志

---

## 2026-08-02 全量部署 + 安全加固 + 代码优化 + 清理

### 版本：v2.0

### GitHub 仓库
| 操作 | 说明 |
|------|------|
| 仓库地址 | `git@github.com:zj123499/xinchen-ERP.git` |
| 分支 | main (c63c8fc) |
| 私有化 | ⚠ 需手动操作: https://github.com/zj123499/xinchen-ERP/settings → Danger Zone → Change visibility → Make private |

### 安全加固（三轮审计）
| 类别 | 修复项 |
|------|--------|
| Cookie | httpOnly: false → true（5处），secure 根据 x-forwarded-proto 动态 |
| 密码 | 所有硬编码密码消除（Xc@123456、admin123、DEFAULT_PASSWORD→随机生成） |
| 加密 | AES-256-GCM 加密 Server/Site/CompanyTemplate 密码（新增 ENCRYPTION_KEY 环境变量需配置） |
| 速率限制 | 登录 IP+用户名 每分钟5次，改密同用户每分钟3次 |
| 密码策略 | 最低8位+至少1个字母+至少1个数字 |
| 权限 | 66个 API 路由新增 requirePermission |
| 安全响应头 | X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Referrer-Policy, Permissions-Policy |
| 开放重定向 | 2 处 add isValidRedirect() 白名单 |
| SSRF | AI Gateway URL 白名单 + 生产禁用 localhost |
| 路径遍历 | storage.ts category 参数清理 |
| 错误泄露 | error.message 16处→通用消息 |

### 代码优化
| 类别 | 变更 |
|------|------|
| getContext 去重 | 137 个 API 路由统一为 getServerContext（减少~1000行） |
| 包精简 | next-auth 移除，@types/crypto-js+prisma→devDeps（15→12 dep） |
| CSS 复用 | globals.css 新增 11 个 @utility 类 |
| 共享组件 | Modal.tsx、LoadingSpinner.tsx、useAbortController Hook |
| React.memo | Sidebar 包裹 memo |
| echarts | 动态导入（~1MB 不计首屏） |
| loading.tsx | (main) 路由级骨架屏 |
| DB 索引 | +10 个关键索引（Lead/Contract/Application/FollowUp/Partner/User） |

### 部署过程
| 步骤 | 结果 |
|------|:--:|
| 源码打包（tar.gz） | 464KB |
| 上传至服务器 | ✅ |
| 备份 .env.production | ✅ |
| 解压 + 恢复环境变量 | ✅ |
| Docker compose build --no-cache | ✅ |
| Docker compose up -d | ✅ |
| 容器健康检查 | ✅ `{"status":"ok"}` |
| Prisma db push --accept-data-loss | ✅ `in sync` |
| 前端页面验证 | ✅ HTTP 200, 13KB, 25ms |

### 服务器清理
| 项目 | 清理前 | 清理后 |
|------|--------|--------|
| 磁盘总使用 | 31GB | 17GB |
| 磁盘使用率 | 27% | 15% |
| 释放空间 | — | **14GB** |
| 清理项 | Docker builder cache、dangling images、stopped containers、旧 journal 日志(744MB)、macOS 元数据(._ .DS_Store)、.next 缓存、/tmp 临时文件 | |

### 当前容器
| 名称 | 状态 | 端口 |
|------|:--:|------|
| xinchen-erp | ✅ healthy | 3000 |
| 1Panel-postgresql-Bq8Z | ✅ healthy | 127.0.0.1:5432 |

### ⚠ 待办
- [ ] 将 GitHub 仓库设为私有: https://github.com/zj123499/xinchen-ERP/settings
- [ ] 生产环境新增环境变量: `ENCRYPTION_KEY=$(openssl rand -hex 32)`（AES 加密用）

---

## 2026-08-02 全面代码审计 + 文档清理

### 审计结果
| 审计项 | 结果 |
|--------|:--:|
| 死代码（getMenuTreeByRoles/DEPARTMENT_MENUS） | 无残留 |
| 未使用的 useRouter 导入 | 无 |
| 旧 middleware 引用 | 无 |
| 孤立 API 路由 | 无 |
| console 日志 | 118+ 处（dingtalk 保留，页面保留） |

### 文档清理
| 文件 | 操作 |
|------|------|
| 实现进度对照清单.md | 完全重写：菜单表/模块进度/架构决策/技术栈 |
| 开发原则.md | 版本号 v1.0→v1.1，方案 v3.1→v3.2 |
| 操作日志.md | 本次审计记录 |

---

## 2026-08-02 菜单整合

### 审计清理
| 项目 | 结果 |
|------|:--:|
| 编译代码 AI 残留 | 无 |
| DB menus 表 AI 残留 | ✅ 已清理 4 条 |
| DB role_menus AI 残留 | ✅ 已清理 17 条 |
| `ai:use` 权限 | 无 |

### 工作台模块权限
- 6 个 `dashboard:*` 权限码已在 `角色权限→权限配置→[工作台]` 中
- 管理员可控制各角色在工作台看到的数据模块
- seed 补全：`dashboard:leads/contracts/finance/students/applications/visits`
- groupName: `工作台模块`

### 菜单手动排序
- `PUT /api/menus/reorder` — 批量更新排序
- `filterMenusByCodes` — 按 codes 数组顺序排列
- 角色权限页「编辑排序」↑↓按钮

---## 2026-08-01 GitHub SSH 修复

### 问题
本地代理劫持 DNS（`github.com`→`198.18.0.66`）和 SSH 22 端口。

### 修复
写入 `~/.ssh/config`，GitHub 走 SSH over 443：
```
Host github.com
    Hostname ssh.github.com
    Port 443
    User git
```

---

## 2026-08-01 按《部门架构.docx》重构菜单

### 核心原则
- MENU_TREE 与文档完全一致
- 菜单可见性 100% 由管理员在「角色权限→菜单配置」中控制
- 代码不硬编码任何部门可见规则

### 变更
| 序号 | 变更项 |
|:--:|------|
| 1 | 交付部 → **文书部**（改名） |
| 2 | **客户管理** 从咨询部子菜单提升为**独立一级菜单** |
| 3 | 咨询部不再包含客户管理子菜单 |
| 4 | 部门菜单（DEPARTMENT_MENUS）移除，全部改为权限驱动 |
| 5 | Sidebar 恢复 `filterMenusByCodes` 权限过滤 |
| 6 | 新增 `customer` 图标（UserCheck） |

### 菜单结构（与文档一致）
```
工作台
新媒体部 → 线索管理、新媒体账号
市场部   → 线索管理、待跟进、意向客户、已签约客户、无意向客户
网络部   → 线索管理、站群管理(站点/模板/服务器)
咨询部   → 线索管理、待跟进、意向客户、已签约客户、无意向客户
客户管理 → 线索流转、回访记录、回访计划、触点管理、归因结果
文书部   → 学生档案、申请管理、签证管理
合作方管理 → 合作院校、合作方
财务部   → 收款/收入/应收/应付/发票/退费/成本/提成/薪资/报销/渠道ROI/利润
合同管理
扩展业务 → 租房管理、境外服务
产品资源 → 国家/院校/专业/产品/套餐
风险管理 → 风险看板、风险规则
数据大屏
系统设置 → 员工/组织/角色/钉钉/字典/配置/审计/操作/登录日志
```

> ROLE_DEPARTMENT_MAP 保留，仅用于 WorkRecord 业绩记录的部门归属。

## 2026-08-01 代码清理 + 架构收尾

### 代码清理
| 文件 | 清理内容 |
|------|---------|
| `menus.ts` | 移除死代码 `flattenMenus()`、`filterMenusByCodes()`（无引用） |
| `employees/page.tsx` | 移除未使用的 `useRouter` 导入 |
| `partners/page.tsx` | 移除未使用的 `useRouter` 导入 |
| `commissions/page.tsx` | 移除未使用的 `useRouter` 导入 |

---

## 2026-08-01 部门架构混合方案

### 部门拆分
- 市场部拆为 **新媒体部** / **市场部** / **网络部** 三个独立部门
- 每个部门的角色登录后只看到本部门菜单
- 侧边栏 Logo 下方显示部门名称（蓝色标签）

### 角色→部门映射
| 部门 | 角色 | 菜单项数 |
|------|------|:--:|
| 新媒体部 | 新媒体主管/运营 | 3 |
| 市场部 | 市场专员 | 3 |
| 网络部 | 网络运营/直播 | 4 |
| 咨询部 | 升学顾问 | 8 |
| 交付部 | 文书申请 | 4 |
| 财务部 | 财务 | 7 |
| 管理 | admin/总经理/运营总监 | 全量 |

### 业绩记录
- 新增 `WorkRecord` 表（`work_records`），字段：userId/dept/type/amount/quantity/relatedId
- 新增 `GET/POST /api/work-records`
- 管理员看全部，员工只看自己的记录
- 这是后续**自动薪资计算**的基础数据

---

## 2026-08-01 全平台权限管控上线

### API 权限
- 80 个 API 文件添加 `requirePermission()`
- 新增 98 项权限码（seed 24→122）
- 菜单侧新增 `template`/`server` 图标

### 权限码覆盖
站群/营销/产品/回访/风险/AI/扩展/合作方/线索/跟进/系统设置 全覆盖。

---

## 2026-08-01 Nextcloud 文件存储集成

### 方案
- Tailscale 点对点 VPN：ERP 服务器 ↔ Nextcloud 本地机
- 存储后端切换为 Nextcloud WebDAV

### 文件夹规则
```
erp/contract_file/{合同ID}_{合作方名称}/
erp/partner_contract/{合作方ID}_{名称}/
erp/student_material/{学生ID}_{姓名}/
  ├── 成绩单/
  ├── 文书/
  ├── offer/          ← Offer 归入学生文件夹
  └── 其他/
```
- Offer 文件改为存入学生文件夹（`student_material` 统一管理）
- 合同文件删除补 `storage.remove()` 防 Nextcloud 孤文件

---

## 2026-08-01 公司品牌更名

### Logo 更换
- 全局替换为「Newill 新辰未来」Logo
- 位置：登录页 / 侧边栏 / 浏览器标签 / BI大屏 / 页面 Title
- 新增 `public/logo.svg`、`src/app/favicon.ico`

---

## 2026-08-01 AI 模块删除

### 删除
- 菜单中移除 AI 智能组（选校/文书/客服）
- `seed.ts` 移除 `ai:use` 权限码
- API 移除 `requirePermission("ai:use")`
- 数据库清除残留 `role_menus` 中 AI 关联

---

## 2026-07-30~31 关键修复

### 登录 502 修复
- `middleware.ts` 重命名为 `proxy.ts`（适配 Next.js 16）
- 重定向 URL 改用 Host 头，避免 `0.0.0.0` 问题

### 工作台优化
- 静默刷新：60 秒自动拉取数据，不显示 loading
- 移除切回标签页刷新
- 删除快捷操作模块
- 修复 React #418 SSR 水合错误（`toLocaleString` → 正则格式化）

---

## 2026-07-29 全面代码优化与部署

### 代码优化
| # | 文件 | 修改内容 |
|---|------|---------|
| 1 | `applications/page.tsx` | 移除未使用的 `router` 变量和 `useRouter` 导入 |
| 2 | `middleware.ts` | cookie `httpOnly` 统一为 `false`（与 login API 保持一致） |
| 3 | `api/auth/set-cookie/route.ts` | 增加 token 有效性验证，杜绝任意设置 cookie 漏洞 |

### 本月累积更新（2026-07-23 ~ 07-29）

#### 登录系统
- 登录页品牌名改为「新辰未来」
- 修复登录后跳回空白页问题：改用 form POST + 服务端 302 重定向设 cookie
- 中间件支持 `_t` URL 参数作为 cookie 兜底认证

#### 申请与 Offer 管理
- Offer 管理合并到申请管理页面，同一表格行展示 + 展开 Offer 列表
- 新增 Offer 表单内联弹窗，支持上传文件
- Offer 列表状态中文化（已收到/已接受/已拒绝）
- 新增截止日期列（最早 Offer 截止，≤7 天标红）
- 申请意向选中高亮修复（从硬编码 idx===0 改为 selectedIntentionId 追踪）
- **取消申请中的合同关联**：前端表单/表格/API 均移除 contractId 必填

#### 数据字典
- 全局硬编码下拉框解耦到数据字典（跟进方式/咨询业务/学生状态/性别/回访方式/增购类型/租房支付方式/合同状态/合作方类型）
- 分组支持中文显示名称，空分组保留
- 拖拽排序替代编码排序
- 刷新后保持滚动位置和展开状态

#### 审批功能
- 完整删除（菜单/页面/API/Schema/引用全部清理，净减 1146 行）

#### 菜单结构
```
改后：
合作方管理           ← 一级菜单
├── 合作院校
└── 合作方

扩展业务
├── 租房管理
└── 境外服务

交付管理
├── 申请管理          ← Offer 合并到此处
├── 签证管理
└── 回访管理
```

#### 签证
- 删除 `递交日期`/`签证号`/`有效期至`，新增 `批复日期`

#### 新媒体账号
- 新增字段：运营专员（下拉所有在职员工）、手机号、实名认证（输入框）、关联公司、人设风格（数据字典）、风格详情

#### 站群管理
- 新增字段：备案公司、法人代表

#### 租房管理
- 新增支付方式（年付/半年付/三个月）、合作方选择

#### 学生档案
- 新增删除按钮 + 级联清理关联数据

#### 其他修复
- 所有弹窗去除点击空白关闭，仅保存/取消按钮可关闭
- 跟进记录弹窗加关闭按钮
- 工作台自动刷新（每 30 秒 + 切回标签页）
- 服务器 Docker 缓存清理（回收 95GB）
- 删除文书工作台菜单

### GitHub
- 推送失败：SSH/HTTPS 均被防火墙拦截，需手动 `git push origin main`

### 部署状态
- **服务器**: 111.229.72.128:3000
- **容器**: xinchen-erp ✅ 运行中
- **登录**: admin / admin123
