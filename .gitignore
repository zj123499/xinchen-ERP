# ============ 根项目依赖 ============
/node_modules
/package-lock.json

# ============ 构建 / 部署产物 ============
/.server_pull
/.next
/out

# ============ 日志与临时文件 ============
*.log
deploy_ai.log
peek.txt
server_diff.txt
syncfixed.txt

# ============ 操作系统 ============
.DS_Store

# ============ 密钥 / 环境变量（本地敏感信息） ============
.env
.env.local
*.pem
